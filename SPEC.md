# SPEC.md — 容量埋めテスト（StorageFiller）

## 概要

端末の内蔵ストレージにダミーファイルを生成して容量を埋め、端末スペック・稼働時間を確認する個人テスト用Androidアプリ。
配信終了した「容量変更管理」の自製代替。root不使用・標準APIのみ。

## 機能要件

### F1. ダミーファイル生成（埋め込み）
- 保存先: `/storage/emulated/0/StorageFillTest/` 配下に5カテゴリフォルダ
  - `Pictures` / `Movies` / `Music` / `Download` / `Apps`
  - ※Android 10 (API 29) のみ `getExternalFilesDir()/StorageFillTest/` 配下（スコープドストレージ対策）
- 配分: 5カテゴリ均等（各20%）
- ファイルサイズ: 目標容量に応じた単位で大きめに作る（v0.3.0で変更）
  - 目標 1GB未満 → 128MB単位 / 1〜10GB → 512MB単位 / 10GB超 → 1GB単位
  - カテゴリごとの割当を超える端数は、その割当いっぱいの1ファイルに丸める
  - 細かいファイルを大量に作るより高速で、一覧も短くなる
- 埋め量指定（2モード）:
  - 空き容量の%（デフォルト70%。任意の数値入力可）
  - 絶対容量（数値 + MB/GB 単位選択）
  - いずれも現在の空き容量を上限にキャップ
- 進捗表示: 書込済/目標バイト、%、書込速度(MB/s)、ファイル数。通知は100ms間隔に間引く
- 停止ボタンで途中キャンセル可能。**書きかけファイルは削除せず、実サイズに名前を合わせて保存する**（大ボタンの「書き込みを中止して保存」どおりの挙動）
- バックグラウンドスレッドで書込（UIブロックしない）
- 空き容量枯渇時は安全停止

### F2. 画面常時点灯
- 埋め込み中のみ `FLAG_KEEP_SCREEN_ON`（完了/停止で解除）

### F3. 全削除
- アプリが作成したファイルパスを内部ログ（`filesDir/fill_log.txt`）で追跡
- ログに基づき一括削除 → 空フォルダも削除 → 解放バイト数を表示
- ログに無い外部ファイルは削除しない（誤削除防止）
- ログの読み出し時に重複行と実体の無いパスを除く（同名ファイルを作り直すと古い行が残るため）
- 一覧から1件ずつの個別削除にも対応

### F4. 端末スペック表示
- モデル名（MANUFACTURER + MODEL）
- Androidバージョン（RELEASE + APIレベル）
- CPU（/proc/cpuinfo の Hardware 等 + コア数）
- RAM総量（ActivityManager.MemoryInfo）
- ストレージ総容量/空き容量（StatFs）

### F5. 稼働時間表示
- `SystemClock.elapsedRealtime()`（スリープ込み連続稼働時間）
- `SystemClock.uptimeMillis()`（スリープ除外）
- 1秒間隔で自動更新
- フォーマット: 日換算なしの総時間表示「371時間 20分 37秒」形式

### F6. 権限フロー
- API 30+: `MANAGE_EXTERNAL_STORAGE` → 設定画面（ACTION_MANAGE_APP_ALL_FILES_ACCESS_PERMISSION）へ誘導
- API 24〜28: `WRITE_EXTERNAL_STORAGE` 実行時パーミッション
- API 29: 権限不要（アプリ専用外部領域を使用）
- 未許可時は埋め込み開始をブロックし案内表示
- 許可済みのときは「ストレージ権限を許可」ボタンを非表示にする

### F7. ストレージ種類比率の円グラフ（v0.2.0追加）
- ドーナツ型円グラフでストレージ全体のデータ種類比率を可視化（参考アプリ準拠）
- カテゴリ: 写真/動画/音楽/DL/アプリ/その他/システム/**ダミー**/空き
- 計測: `/storage/emulated/0` を再帰スキャンし拡張子で分類（DLはDownload配下、アプリはAndroid/data+obb）
- Android 11+ は `Android/data`・アプリ内部領域を走査できないため、`使用量 − 走査合計 − ダミー` を「システム」セグメントとして加算し、リング合計＝全体容量に揃える（中央の使用率%と見た目を一致させる）
- **ダミーデータは走査対象から除外し、`FillLog` 由来の独立セグメント（赤）にする**（v0.3.0）
- **描画順は「既存データ → ダミー → 空き」**。ダミーが使用済みと空きの境目に来るので、埋めるほど境界が動いて割合を読み取りやすい（v0.3.0）
- **書き込み中も円グラフを連動させる**（v0.3.0）。再スキャンはせず、埋め開始時点の空き容量・ダミー量に書込済みバイト数を加減して描画する（ファイルI/Oゼロ）
- バックグラウンドスキャン＋進捗表示。起動時・埋め/削除後・更新ボタンで再計測
- 統計パネル: 全体/実データ/ダミー/空き/稼働時間（参考アプリの右側統計を再現）

### F8. 参考アプリ風UI（v0.2.0追加）
- クリーム色グラデーション背景、白丸角ボタン
- ヘッダ: 「ストレージ管理 / 容量割り当てツール」中央寄せ
- 保存先フォルダチップ: APP(紫)/DL(青)/写真(緑)/動画(橙)/音楽(緑青) の色付きトグル。選択カテゴリに均等配分
- 埋め開始トリガ: 割合プリセット(30/60/70%) と固定サイズ(100MB/1GB/5GB/10GB/30GB/50GB) のボタン群
- 大ボタン: 待機時グレー「書き込みを中止して保存」→ 書込中は赤で停止ボタン化
- 作成済みダミーデータ一覧: [カテゴリ] ファイル名+サイズ+個別削除、全削除ボタン付き
- 一覧は**ヘッダのタップで開閉し、デフォルトは閉じた状態**（縦に伸びすぎるため）。ヘッダに件数と合計容量を出す（v0.3.0）
- ダミーファイル名はカテゴリ別拡張子（写真.jpg/動画.mp4/音楽.mp3/DL.bin/アプリ.bin）で円グラフ分類に自然反映

### F9. ドット絵ゲーム風UI（v0.4.0追加）

- 全体をドット絵ゲームの画面デザインにする。**レイアウト構造は v0.3.0 のまま、見た目だけ差し替える**
- 配色は白地＋黒太枠＋赤アクセント（ポケモン赤のゲーム画面寄り）
  - 背景 `#FFFFFF` / 枠・文字 `#000000` / アクセント `#D82800` / 淡色 `#A8A8A8`
  - 画面のクローム（背景・枠・文字）は白黒赤に統一し、カテゴリ色だけ8bit風の原色を使う
- フォント: PixelMplus 12（M+ FONT LICENSE）を `assets/fonts/` に同梱し、起動時にビューツリーを辿って全テキストへ適用。太字指定は Bold 体に差し替える
- 枠: 角丸ゼロ・太い黒枠。タイトル／統計パネル／端末情報はウィンドウ枠で囲む
- ボタン: 白地＋黒枠。全削除は赤枠、書き込み中の停止ボタンは赤地＋黒枠
- カテゴリチップ: 角丸ゼロ・黒枠。選択中はカテゴリ色で塗り、未選択は白地
- 円グラフ: アンチエイリアスを切り、正方形セルのグリッドを1マスずつ塗ってドット化する
- **表示文言は変更しない**（デザインのみの変更）

### F10. アプリアイコン（v0.4.1追加）

- モンスターボール風の円。**上半分が赤（アクセント `#D82800`）／下半分がねずみ色（`#A8A8A8`）／中央の白い丸ボタンに「100%」**
- 太い黒枠・黒帯、アンチエイリアスなしでアプリ本体のドットUIに揃える。文字は PixelMplus 12 Bold
- レガシー（API 24-25）用 PNG を mipmap 5密度、アダプティブ（API 26+）用に `ic_launcher_foreground` と背景色 `ic_launcher_background`（白）
- アダプティブは 108dp キャンバスの中央 72dp が可視域。ボールは pad 0.18 で少し内側に収め、円マスクでも黒枠が欠けないようにする
- **内部パーツ（黒枠・帯・中央ボタン）はキャンバスサイズではなくボール直径基準で算出する。** キャンバス基準だと余白の違うアダプティブ版で比率が崩れる

### F11. アプリ内アップデート（v0.5.0追加）

- 端末情報の下に「アップデート」欄を置き、**ボタンを押したときだけ**確認する（自動確認はしない）
- 取得元: `https://api.github.com/repos/{UPDATE_REPO}/releases/latest`（公開リポジトリなので認証不要）
  - リポジトリは `BuildConfig.UPDATE_REPO` で持つ。移設時はここだけ変える
  - 未認証のGitHub APIは60回/時（IP単位）。手動ボタンなら問題にならない
- タグ名（`v0.5.0`）から `v` を除き、`BuildConfig.VERSION_NAME` と**数値で**比較する
  - 文字列比較だと `0.10.0 < 0.9.0` になるため、ドット区切りで数値化して比べる
- 新しければ「ダウンロードしてインストール」を表示 → `cacheDir/update/` に保存 → インストーラーを起動
- 通信は `HttpsURLConnection`、JSONは `org.json`。**どちらもAndroid標準なので依存ライブラリは増やさない**
- APKの受け渡しは自前の `ApkProvider`（`ContentProvider`）。API 24+ は `file://` を外部に渡せないため `content://` が必要で、androidx の FileProvider は使わない方針のため自作する
  - 公開範囲は `cacheDir/update` 配下のみ。`..` やパス区切りを含むURIは拒否する
- API 26+ は `canRequestPackageInstalls()` を確認し、未許可なら `ACTION_MANAGE_UNKNOWN_APP_SOURCES` へ誘導
- 追加権限: `INTERNET` / `ACCESS_NETWORK_STATE` / `REQUEST_INSTALL_PACKAGES`

### 署名（v0.5.0以降）

- **専用のリリース鍵で署名する。** Androidは署名の異なるAPKを上書きできないため、アップデート機構は同一鍵で署名し続けることが前提になる
- 鍵はリポジトリ外（`~/.android/storage-filler-release.jks`）。パスと3つのパスワードは `keystore.properties`（gitignore済み）に置く
- `keystore.properties` が無い環境では署名設定を読み込まず、`assembleDebug` だけが通る
- **鍵ファイルとパスワードの両方を失うと、既存インストールへの上書き更新が永久にできなくなる**


## 非機能要件

- minSdk 24（Android 7.0）/ targetSdk 35 / compileSdk 35
- Kotlin + プレーンActivity + XMLレイアウト（autoflow-apk と同規約。依存ライブラリ最小限・androidx不使用）
- AGP 8.7.3 / Kotlin 2.0.21 / Gradle 8.9 / JDK 21
- パッケージID: `jp.own.storagefiller`
- アプリ名: 容量埋めテスト
- 同梱フォント: `assets/fonts/PixelMplus12-{Regular,Bold}.ttf`（M+ FONT LICENSE。ライセンス全文を `assets/fonts/LICENSE_M+` に同梱）。APKは約0.9MB→1.8MBになる

## 対象外スコープ

- カテゴリ別の個別比率指定（均等配分のみ）
- MediaStore 経由の公共フォルダ直接登録
- Google Play 配信（個人サイドロードのみ）
- Android 6.0以下のサポート
- root権限を要する操作

## 配布

- `gradlew assembleRelease` でAPK生成（v0.5.0以降。リリース鍵で署名される） → `releases/StorageFiller-v{version}.apk`
- git タグ `storage-filler-v{version}` → push → GitHub Releases にAPK添付
